using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WalkMusic : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(this.gameObject, 0.01f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
